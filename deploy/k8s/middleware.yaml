apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: novel-translator-auth
  namespace: novel-translator
spec:
  basicAuth:
    # htpasswd content injected by the CD workflow from BASIC_AUTH_HTPASSWD.
    secret: novel-translator-basic-auth
